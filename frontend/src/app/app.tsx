import React from 'react';
import { BrowserRouter as Router, Switch, Route } from 'react-router-dom';
import './app.css';

const App = () => {
    return (
        <React.Fragment>
            <Router>
            </Router>
        </React.Fragment>
    );
};

export default App;
